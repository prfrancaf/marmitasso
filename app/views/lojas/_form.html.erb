<%= form_with(model: loja, local: true) do |form| %>
  <% if loja.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loja.errors.count, "error") %> prohibited this loja from being saved:</h2>

      <ul>
      <% loja.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :loja_nome %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo, id: :loja_tipo %>
  </div>

    <div class="field">
      <%= form.label :cep %>
      <%= form.text_field :cep, id: :loja_cep %>
    </div>

    <div class="field">
      <%= form.label :tempo %>
      <%= form.text_field :tempo, id: :loja_tempo %>
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
